[{% for product in results -%}
    {{ {id: product.id, label: product.name, value: product.name}|json_encode|raw }}
    {# use "value" instead of "id" key, if you use jquery-ui #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]